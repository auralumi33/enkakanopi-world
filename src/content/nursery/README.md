# the nursery beds (empty on purpose)

Seeds land here as `.md`/`.mdx` files — a title, an optional one-line
fragment, a stage (default `seed`). See the `seed` schema in
`src/content.config.ts`.

No seed is authored by an assisting hand. They arrive in Rika's hand,
or not at all. This README is excluded from the collection.
