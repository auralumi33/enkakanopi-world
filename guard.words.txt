# guard.words.txt — the public off-register list
#
# One word or phrase per line; blank lines and # comments are ignored.
# `npm run guard` (also run in CI) refuses to let any of these into the repo.
# Matching is whole-word, case-insensitive, and deliberately specific — e.g.
# "astrology", never bare "astro" — so it never trips the Astro framework or
# CSS "transition".
#
# The garden's PRIVATE register guards (names, places, and any vocabulary that
# would triangulate a person or a venture) live in an UNTRACKED sibling file,
# `guard.words.local.txt`, so those terms are enforced locally but never shipped.
# See ARCHITECTURE.md.

# — firewalled: astrology / divination register (this venture does not write here) —
astrology
astrological
horoscope
zodiac
natal
retrograde
clairvoyant
clairvoyance
numerology
tarot
bazi
chakra

# — style: words the writing avoids (AI-tells / filler) —
delve
seamlessly
